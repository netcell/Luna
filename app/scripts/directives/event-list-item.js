'use strict';

angular.module('lunaApp')
  .directive('eventListItem', function () {
    return {
      restrict: 'E',
      link: function postLink(scope, element, attrs) {
        
      }
    };
  });
